from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This project contain the calculator with the sum, rest, division an multiplication functions',
    author='Noel H.S',
    license='',
)
